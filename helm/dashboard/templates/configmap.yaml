apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dashboard.fullname" . }}-config
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
data:
  QUERY_SERVICE_URL: {{ .Values.env.queryServiceUrl | quote }}
